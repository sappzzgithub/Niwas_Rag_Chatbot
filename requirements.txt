# ─────────────────────────────────────────────────────────────────
# Multimodal RAG — Requirements
# Install: pip install -r requirements.txt
# Python 3.10+ required
# ─────────────────────────────────────────────────────────────────

# ── PDF Parsing ───────────────────────────────────────────────────
pymupdf4llm>=0.0.17          # Markdown extraction (wraps PyMuPDF)
PyMuPDF>=1.24.0              # fitz — image extraction, page rendering

# ── NLP / Chunking ────────────────────────────────────────────────
langchain-text-splitters>=0.2.0  # RecursiveCharacterTextSplitter
langchain-core>=0.2.0

# ── Embeddings ────────────────────────────────────────────────────
sentence-transformers>=3.0.0     # BAAI/bge-base-en-v1.5
torch>=2.1.0                     # backend for sentence-transformers

# ── Vector Database ───────────────────────────────────────────────
chromadb>=0.5.0

# ── Vision Providers (choose at least one) ────────────────────────
google-generativeai>=0.7.0       # Gemini vision + LLM
# groq>=0.9.0                    # Groq LLaVA + LLaMA (uncomment if needed)
Pillow>=10.0.0                   # Image loading for Gemini provider

# ── HTTP (Ollama provider) ────────────────────────────────────────
requests>=2.31.0

# ── Environment ───────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── UI ────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Dev / Testing ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0